/**
 * Дополнения к странице магазина, которые нельзя собрать из фида.
 *
 * Search Console показала, что к FARFOR почти половина показов сайта приходит
 * по запросам «промокод фарфор + город» (Дубна, Белогорск, Рязань,
 * Североморск…), а страница про города ничего не говорила. Коды Perfluence у
 * FARFOR общие для всей сети, без привязки к региону, — это и объясняем,
 * перечисляя города, которые реально ищут.
 */
export interface StoreExtra {
  /** Раздел «в вашем городе»: города берём из запросов Search Console. */
  cities?: { heading: string; text: string; list: string[] };
  /** Дополнительные вопросы в FAQ (попадают и в разметку FAQPage). */
  faq?: { q: string; a: string }[];
}

const STORE_EXTRAS: Record<string, StoreExtra> = {
  farfor: {
    cities: {
      heading: "Промокоды FARFOR в вашем городе",
      text:
        "Коды на этой странице не привязаны к городу: скидка применяется в любом ресторане сети, куда FARFOR доставляет заказ. Отдельных «городских» промокодов у FARFOR нет — если на другом сайте код подписан как «для Рязани» или «для Дубны», это тот же общий код. Город выбирается на сайте или в приложении до того, как вы соберёте корзину; сумма, от которой действует скидка, от города не зависит.",
      list: [
        "Москва",
        "Белгород",
        "Белогорск (Амурская область)",
        "Джанкой",
        "Дубна",
        "Калининград",
        "Курск",
        "Липецк",
        "Нефтекамск",
        "Октябрьский (Башкортостан)",
        "Омск",
        "Рязань",
        "Самара",
        "Североморск",
        "Туймазы",
        "Челябинск",
        "Ярославль",
      ],
    },
    faq: [
      {
        q: "Работает ли промокод FARFOR в моём городе?",
        a: "Да, если FARFOR доставляет в ваш город: коды на этой странице общие для всей сети и не зависят от региона. Выберите город на сайте FARFOR, соберите корзину и вставьте код при оформлении.",
      },
      {
        q: "Есть ли у FARFOR промокод на сегодня?",
        a: "Все коды на этой странице действуют сегодня — срок указан в карточке каждого купона. Истёкшие коды убираются со страницы автоматически, поэтому отдельно искать «промокод на сегодня» не нужно.",
      },
    ],
  },
};

export function getStoreExtra(slug: string): StoreExtra | undefined {
  return STORE_EXTRAS[slug];
}
